UrlsApp.service('SuggestionService', ['$resource', function($resource) {

  //Michael this class should be used for making the API call to webhose
  // I've not wired up the backend to the AnalysisController - just take a look
  // at the json the API serves to wire up. Should be self explanatory...



}]);
